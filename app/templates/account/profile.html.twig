{% extends "base.html.twig" %}

{% block title %}Profil{% endblock %}

{% block body %}

<h1 class="d-flex justify-content-center">Votre profil</h1>
<div class="d-flex justify-content-center flex-column align-items-center">
    <p>Vous êtes connectés en tant que:</p>
    <p><b>{{ app.user.email }}</b></p>
</div>
<div class="d-flex flex-wrap justify-content-center gap-3">
    {# <a href="" class="container d-flex btn btn-primary justify-content-center">Modifier mon profil</a> #}
    <a href="{{ path('myAds') }}" class="container d-flex btn btn-warning justify-content-center">Voir mes annonces</a>
    <a href="{{ path('reservationList') }}" class="container d-flex btn btn-success justify-content-center">Voir mes réservations</a>
    <a href="{{ path('myFavorites') }}" class="container d-flex btn btn-primary justify-content-center">Voir mes favoris</a>
    <a href="{{ path('logout') }}" class="container d-flex btn btn-danger justify-content-center">Déconnexion</a>
</div>


{% endblock %}