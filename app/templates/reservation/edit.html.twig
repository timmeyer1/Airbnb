{% extends 'base.html.twig' %}

{% block title %}Edit Reservation{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center">Modifier la réservation</h1>

        {{ include('reservation/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-secondary mt-3" href="{{ path('reservationList') }}">Retourner à la liste</a>
</div>
{% endblock %}