{% block body %}

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date de départ</th>
                <th>Date de retour</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reservation in reservations %}
            <tr>
                <td>{{ reservation.id }}</td>
                <td>{{ reservation.startDate ? reservation.startDate|date('d-m-Y') : '' }}</td>
                <td>{{ reservation.endDate ? reservation.endDate|date('d-m-Y') : '' }}</td>
                <td>
                    <a class="btn btn-secondary" href="{{ path('reservationEdit', {'id': reservation.id}) }}">Éditer</a>
                    <a class="btn btn-danger" href="{{ path('reservationDelete', {'id': reservation.id}) }}">Supprimer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
